import React from 'react';
import './Body.css'
import PropTypes from 'prop-types';

const Body = props => {
    return (
        <div className="body
        ">
            i am the body

        </div>
    );
};

Body.propTypes = {
    
};

export default Body;