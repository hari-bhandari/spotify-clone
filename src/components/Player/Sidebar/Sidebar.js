import React from 'react';
import './Sidebar.css'
import PropTypes from 'prop-types';

const Sidebar = props => {
    return (
        <div className="sidebar">
        I am a d
        </div>
    );
};

Sidebar.propTypes = {
    
};

export default Sidebar;