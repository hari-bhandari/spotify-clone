import React from 'react';
import PropTypes from 'prop-types';

const Player = props => {
    return (
        <div>
            I am logged in
        </div>
    );
};

Player.propTypes = {

};

export default Player;